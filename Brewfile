# Brewfile — run `brew bundle` (or let script/bootstrap offer to) to install.
# Starter list; trim/extend to match what's actually on your machines.

brew "git"
brew "zsh"
brew "vim"
brew "tmux"
brew "tree"
brew "the_silver_searcher"  # ag — fast code search
brew "fd"  # fast, user-friendly alternative to find
brew "mise"
brew "node"
brew "python"
brew "beads"  # `bd` — dependency-graph issue tracker for AI coding agents
brew "glow"  # CLI markdown renderer
brew "lazydocker"  # TUI for Docker

tap "stablyai/orca"
cask "stablyai/orca/orca"  # Orca — Stably's agentic dev environment (ADE)
