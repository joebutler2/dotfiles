# Package names for apt-based distros (Debian, Ubuntu, ...). Mirrors Brewfile.
git
zsh
vim
tmux
tree
nodejs
python3

# glow (CLI markdown renderer) isn't in default apt repos - install via
# Charmbracelet's own repo (https://github.com/charmbracelet/glow#installation)
# or Homebrew on Linux if you have it.
